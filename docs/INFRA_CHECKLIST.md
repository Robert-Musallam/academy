# HUMAN GATE 16 — infrastructure and provider decision

Local implementation stops here. Robert completes these five setup steps. Never paste secrets into this session; enter them only in the relevant provider dashboards.

1. **Create a new Supabase project named `academy`.** Do not reuse gcvpmOS, blackboard or any existing business project. Keep the `private` schema out of Data API exposure. Copy only its project ref for the eventual resume reply. Configure the final site URL and allowed `/auth/callback` redirect, disable public signup, and configure production SMTP so rostered trainees can receive their links. Migrations and the initial admin roster bootstrap occur after this gate; bootstrap instructions must use your real auth user ID and target tenant, not test fixtures.
2. **Create a Vercel project from `Robert-Musallam/academy`.** Use the repository root and its pinned pnpm build. Supply the eventual URL, with production tied to `main`; no main merge occurs before this gate. Select a plan whose cron timing meets the intended Monday 07:00 Denver delivery; Vercel cron timing is plan-dependent. Configure the environment before the production launch.
3. **Set the variables named in `.env.example` in Vercel.** Inventory: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, LLM_PROVIDER, LLM_API_KEY, LLM_SIM_MODEL, LLM_GRADER_MODEL, TEAMS_WEBHOOK_URL, CRON_SECRET, LLM_SIM_INPUT_USD_PER_MILLION, LLM_SIM_OUTPUT_USD_PER_MILLION, LLM_GRADER_INPUT_USD_PER_MILLION, LLM_GRADER_OUTPUT_USD_PER_MILLION. Use the new Academy project's values. The service-role and other private settings must never receive NEXT_PUBLIC prefixes. Do not configure ACADEMY_E2E in Vercel. The price fields are numeric USD per million input/output tokens for the selected models; keys/webhooks/cron credentials remain dashboard-only.
4. **Create a Teams incoming webhook workflow in the chosen RNB channel and set TEAMS_WEBHOOK_URL.** Use Teams Workflows / Power Automate with “When a Teams webhook request is received” and an action that posts the incoming Adaptive Card attachment content. Configure its trigger so the server can call the generated URL without interactive Microsoft sign-in; protect that generated URL as a secret. Assign a maintained owner and co-owner, and select a channel whose audience is authorized to see trainee progress. Academy sends the `type: message` / `attachments` / `application/vnd.microsoft.card.adaptive` envelope, not a plain text-only template. Keep the channel scoped to RNB. Do not paste the URL here.
5. **Choose the LLM provider and models.** Read PROVIDER_OPTIONS.md, choose a configuration (or name another), and set LLM_PROVIDER, LLM_API_KEY, LLM_SIM_MODEL and LLM_GRADER_MODEL plus matching price fields in Vercel. The current production adapter is OpenAI; another provider requires its adapter before deployment. No real-provider quality claim has been made from mock tests.

## Resume signal

Reply **GATE 16 DONE** with the **Vercel URL** and **Supabase project ref**, and identify your chosen provider/simulation/grader models. Confirm the Teams workflow and dashboard variables are configured without sharing their values. Node 17 then handles production migrations, content sync and the reviewed merge/deploy; Node 18 verifies one real appointment, grade and authorized Teams post. Human confirmation of the actual Teams card remains required.

## Setup notes and sources

Microsoft's retirement notice says legacy Office 365 connectors stopped functioning after the May 18–22, 2026 rollout. The Workflows setup above is the current replacement for the kickoff's original “incoming webhook” instruction. [Microsoft retirement notice](https://devblogs.microsoft.com/microsoft365dev/retirement-of-office-365-connectors-within-microsoft-teams/), [incoming webhook and Adaptive Card setup](https://learn.microsoft.com/en-us/microsoftteams/platform/webhooks-and-connectors/how-to/add-incoming-webhook).

Vercel cron sends CRON_SECRET as bearer authorization. The repository includes two UTC schedule candidates; the route allows only 07:00 Denver, so daylight-saving changes do not shift the intended delivery hour. [Vercel cron management](https://vercel.com/docs/cron-jobs/manage-cron-jobs).

A Workflow accepting HTTP is not sufficient evidence that it posted to the channel. The live smoke must check the workflow run and the actual card. Supabase/Vercel creation, SMTP and real model billing have not been exercised locally.
